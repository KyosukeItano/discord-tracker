{
  "name": "gateway-tracker",
  "version": "1.0.0",
  "description": "Discord Gateway API tracker for voice channel monitoring",
  "main": "gateway-tracker.js",
  "bin": "gateway-tracker.js",
  "scripts": {
    "test:gateway": "node gateway-tracker.js",
    "prebuild": "node prebuild.js",
    "build": "pkg . --targets node18-win-x64 --output gateway-tracker.exe"
  },
  "dependencies": {
    "ws": "^8.18.3",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  },
  "pkg": {
    "assets": [
      "config.example.json"
    ]
  }
}
